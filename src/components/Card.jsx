import React from 'react';

const Card = (props) => {
	const cardStyle = {
		height: '10rem',
		width: '12rem',
		border: '1px solid #444'
	};

	return <div style={cardStyle}> </div>;
};

export default Card;
